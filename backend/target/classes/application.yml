spring:
  application:
    name: eduflex-backend

  # Database Configuration (H2 In-Memory for Development)
  datasource:
    url: jdbc:h2:mem:eduflex_db
    username: sa
    password:
    driver-class-name: org.h2.Driver

  # For MySQL, use:
  # datasource:
  #   url: jdbc:mysql://localhost:3306/eduflex_db
  #   username: root
  #   password: root
  #   driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA/Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
    open-in-view: false

  # H2 Console (Access at http://localhost:8080/h2-console)
  h2:
    console:
      enabled: true
      path: /h2-console

  # File Upload Configuration
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
      enabled: true

# Server Configuration
server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

# JWT Configuration
jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000 # 24 hours in milliseconds
  refresh-expiration: 604800000 # 7 days

# CORS Configuration
cors:
  allowed-origins: http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003,http://localhost:3004,http://localhost:5173,http://localhost:4200
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  exposed-headers: Authorization
  allow-credentials: true

# Application Configuration
app:
  upload-dir: ./uploads
  file-storage-location: ./uploads
